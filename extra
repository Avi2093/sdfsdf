string html = File.ReadAllText("path/to/template.html");
string imagePath = "path/to/image.png";

html = html.Replace("{{FirstName}}", firstName)
           .Replace("{{LastName}}", lastName)
           .Replace("{{ImagePath}}", imagePath);
