<filters>
                <!-- Only log when there's an exception or HTTP status code is 401 or 204 -->
                <when condition="'${exception}' != '' 
                                  or '${aspnet-request-status-code}' == '401'
                                  or '${aspnet-request-status-code}' == '204'" />
            </filters>
